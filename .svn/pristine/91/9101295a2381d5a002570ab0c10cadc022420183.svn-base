package com.virtusa.gto.pb.core;

import java.util.Random;

import com.virtusa.gto.pb.exceptions.pBException;
import com.virtusa.gto.pb.format.Formatter;
import com.virtusa.gto.pb.validate.Validator;

public class Main {

    /**
     * TODO Method Description as a Single Sentence.
     * 
     * @param args
     */
    public static void main(String[] args) {
        PhoneBook pb = new PhoneBook();
		
		// @fixme S1 haba haba haba Severity 1 defect 
		// @fixme S2 haba haba haba Severity 2 defect 
        // continue review comment
		// @fixme S3 haba haba haba Severity 3 defect 
		// @fixme haba haba haba what Severity will this be? (should be Severity 1)
        System.out.println(pb.isValidEmail("omegasilva@gmail.com"));
        System.out.println("Class Name : " + pb.getClass());
		// @fixme S3 this comment content is duplicated
        Formatter f = new Formatter();
        System.out.println(f.formatName("omega silva")); // @fixme S3 hoo I see a println!
    }
    
    public static void copyPasteMethod1() {
		// @fixme S3 this comment content is duplicated
		// @fixme S3 this comment content is duplicated but the priority is different
        Random randObj = new Random();
        double randomValue = randObj.nextDouble();
        
        // This is the mathematical modle we use
        double value = Math.round(Math.sqrt(Math.pow(randomValue, 2.4)) * randomValue) % 7.0 * randomValue * 1000.0;
        
        // Conditions
        if (value <= 1.0) {
            System.out.println("Less than or equal to 1"); 
        } else if (value <= 10.0) {
            System.out.println("Greater than 1, less than or equal to 10");
        } else if (value <= 30.0) {
            System.out.println("Greater than 10, less than or equal to 30");
        } else {
            System.out.println("Greater than 30");
        }
		// @fixme S2 this comment content is duplicated but the priority is different		
    }
}
